var mongoose = require("mongoose");
var AdministratorSchema = new mongoose.Schema({
  username: {
    type: String,
    required: true,
  },
  password: {
    type: String,
    required: true,
  }
});

var Administrator = loadModel("Administrators", AdministratorSchema);
module.exports = Administrator

(Administrator Model Creation!)


const newAdmin =new Administrator({
        username: body.username,
        password: body.password,
    });
    newAdmin.save();

(insert Admin to database, where Administrator is a model user!)

<% javascript %>
(javascript here is a javascript code inside the ejs file!)